name: "bi_dashboard"
version: "1.0.0"
config-version: 2

profile: "bi_dashboard"

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  bi_dashboard:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
